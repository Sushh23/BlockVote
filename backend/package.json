{
  "name": "backend",
  "version": "1.0.0",
  "description": "Hardhat backend for the DAO Voting DApp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "compile": "hardhat compile"
  },
  "keywords": [
    "hardhat",
    "solidity",
    "blockchain"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.4", 
    "dotenv": "^16.3.1"                           
  }
}